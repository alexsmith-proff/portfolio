import Title from "@/app/components/title/title";
import { FC } from "react";

const MainAbout: FC = ({ }) => {
    return (
        <div id="main_about">
            <Title title="Кратко о себе" />
        </div>
    )
}

export default MainAbout